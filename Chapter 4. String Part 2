今天的任务其实是很轻松的，我看了看，很多东西都是可以跳过的，二周目的时候开始刷。
我发现咱们的这个训练营是可以免费回头二刷的，到时候看看这几道题吧

今儿唯一的一道题：
151 Reverse Words in a String
这道题跟之前的题目不一样，这道题只是要求反转单词的顺序，而不是字母的顺序。我目前没啥上来就有的思路，可能还是要用到双指针法，但是具体实现还是得考量一下

首先是python中最精简的版本：
class Solution:
    def reverseWords(self, s: str) -> str:
        s = s.strip()
        s = s[::-1]
        s = ' '.join(word[::-1] for word in s.split())
        return s
这里面strip()函数用来移除字符串开头和结尾的空白字符，包括空格，制表符，换行符等
lstrip()是移除开头的空白字符，rstrip()是移除结尾的空白字符

s[::-1]切片语法：
  切片语法；用于截取序列（如字符串，列表，元组）中特定部分的语法，通过切片可以获取序列中的一个子序列
  sequence[start:stop:step]
  start包含在结果中，stop不包含在结果中

split():  将字符串按照指定的分隔符拆分成一个列表，默认以空格（包括连续的空格和其他空白字符，如换行符，制表符等）作为分隔符
应当注意的是，返回的是列表

join函数之前介绍过了，就是将迭代对象转化为一个字符串

下面试试题解中的第二种方法，也就是双指针
双指针这个解法有点，出乎我想象的无聊了。因为他是将每个单词看做一个元素了，通过split就可以通过空格将单词分到List中当作不同的元素，然后通过两个指针来反转元素，emmm
