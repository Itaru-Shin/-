今儿的部分并没有理论知识的讲解，一上来就是刷题，看来更多的是需要经验。

344. Reverse String
这里面提到一点：如果题目关键的部分直接用库函数就可以解决，建议不要用库函数
如果库函数仅仅是解题过程中的一小部分，并且很清楚这个库函数的内部实现原理的话，可以考虑用库函数
题目的思路是非常直接易懂的，跟之前反转链表有相似的难度

541 Reverse String 2
刷题中有一些问题是模拟，意思是给你规定了一种规则，你要实现这种算法
这里面有个很好的思想：当需要固定一段一段去处理字符串的时候，要想想在for循环的表达式上做文章

这里面也学到了python的一个非常好的特性：list的边界检查：
 字符串s = 'abc'，如果使用s[0:999] ===> 'abc'。字符串末尾如果超过最大长度，则会返回至字符串最后一个值，这个特性可以避免一些边界条件的处理。

这里面还有一点需要注意，一开始将s转化为list了，但是最后返回的是string，因此需要再变回来
这里是用的''.join()，join的函数是可以将一个可迭代的对象链接成字符串，前边空的字符串是为了能够使用join
