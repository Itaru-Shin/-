56. Merge intervals 合并区间
先排序，让相邻区间尽可能重叠在一起
基本还是一个重叠区间的题目

738. Monotone increasing digits 单调递增的数字
第一次看题没看明白，原来是要求数字的每一位是单调递增的

这里面需要注意的是什么？遍历的顺序，要从后向前遍历

然后需要注意的点就是，当判断到前一位比后一位大的时候，前一位减一，然后后面所有的数全部变成9

这里面还需要注意的事情是，需要将数字转变为字符串进行操作，然后再换回来，是很常用的数字操作

968. binary tree cameras监控二叉树

题目感觉挺有意思的，安装监控，可以监控到临近node

有叶子结点，肯定要在叶子结点的父结点放摄像头

遍历顺序：后序遍历

结点状态：
0: 无覆盖
1: 有摄像头
2: 被覆盖

空结点应该给什么状态呢？有覆盖的状态

贪心思路：跳两个点放摄像头

向上返回的是什么？是当前node的状态

看了视频之后觉得很清楚了。这道题好有趣
