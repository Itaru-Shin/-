链表的定义：通过指针串联在一起的数据结构
基本单位：节点
节点的组成：data和指针域
第一个节点，也就是头节点，是head

链表的类型：
单链表：指针域只能指向节点的下一个节点
双链表：每一个节点有两个指针域，一个指向下一个节点，一个指向上一个节点（又能向前查询，又能向后查询）
循环链表：链表首尾相连

数组在内存中是连续分布的，但是链表在内存中不连续分布

链表的程序层面的定义：要会写，需要定义data，next，其中next的数据类型是ListNode
