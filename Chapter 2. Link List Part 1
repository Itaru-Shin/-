链表的定义：通过指针串联在一起的数据结构
基本单位：节点
节点的组成：data和指针域
第一个节点，也就是头节点，是head

链表的类型：
单链表：指针域只能指向节点的下一个节点
双链表：每一个节点有两个指针域，一个指向下一个节点，一个指向上一个节点（又能向前查询，又能向后查询）
循环链表：链表首尾相连

数组在内存中是连续分布的，但是链表在内存中不连续分布

链表的程序层面的定义：要会写，需要定义data，next，其中next的数据类型是ListNode

链表的操作：
  删除节点
  添加节点

性能分析
  数组的插入复杂度是n，因为要把元素挨个覆盖。链表是1，因为只需要指针重新指一下就好了
  数组的查询复杂度是1，可以直接按照index进行查询。链表是n，因为需要从头开始
  数组适合数据量固定，频繁查询，少增删的场景
  链表适合数据量不固定，频繁增删，少查询的场景


